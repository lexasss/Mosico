<Window x:Class="Mosico.Views.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mosico.Views"
        mc:Ignorable="d"
        DataContext="{Binding Path=Settings, RelativeSource={RelativeSource Self}}"
        Title="Settings"
        Height="420" Width="200">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Margin" Value="8,4"/>
            <Setter Property="MinWidth" Value="74"/>
        </Style>
        <Style x:Key="Section" TargetType="Label">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="2,8,2,4"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="64"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.ColumnSpan="2"
                   Style="{StaticResource Section}"
                   Content="General"/>
            <Label Grid.Row="1" Grid.Column="0">Cell size, px</Label>
            <TextBox Grid.Row="1" Grid.Column="1"
                     Text="{Binding CellSize}"/>
            <Label Grid.Row="2" Grid.Column="0">Circle size, px</Label>
            <Label Grid.Row="3" Grid.Column="0">Color</Label>

            <Label Grid.Row="4" Grid.ColumnSpan="2"
                   Style="{StaticResource Section}"
                   Content="Size binding"/>
            <Label Grid.Row="5" Grid.Column="0">Telemetry field</Label>
            <Label Grid.Row="6" Grid.Column="0">Scale</Label>
            <Label Grid.Row="7" Grid.Column="0">Max size, px</Label>
            <Label Grid.Row="8" Grid.Column="0">Damping rate</Label>

            <Label Grid.Row="9" Grid.ColumnSpan="2"
                   Style="{StaticResource Section}"
                   Content="Offset binding"/>
            <Label Grid.Row="10" Grid.Column="0">Telemetry field</Label>
            <Label Grid.Row="11" Grid.Column="0">Scale</Label>
            <Label Grid.Row="12" Grid.Column="0">Damping rate</Label>
        </Grid>

        <WrapPanel Grid.Row="1"
                   HorizontalAlignment="Center">
            <Button Content="Save"
                    IsDefault="True"
                    Click="OK_Click"/>
            <Button Content="Cancel"
                    IsCancel="True"/>
        </WrapPanel>
    </Grid>
</Window>
